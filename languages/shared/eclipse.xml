<!--
    Requires:
        Properties: eclipse.bin.dir, test.suite.name
        Paths: junit.jar.path, beaver.rt.jar.path
        Targets: gen, clean.gen
-->
<project name="Eclipse">
	<!-- Eclipse targets -->
	<target name="eclipse.build" depends="gen" description="generate all files, let Eclipse build" />

	<target name="eclipse.clean" depends="clean.gen" description="deletes all files produced by build.eclipse" />

	<target name="eclipse.test" depends="eclipse.build" description="runs all JUnit tests, as built by Eclipse">
		<junit fork="true" haltonerror="true">
			<formatter type="plain" usefile="false" />
			<classpath refid="junit.jar.path" />
			<classpath refid="beaver.rt.jar.path" />
			<classpath location="${eclipse.bin.dir}" />
			<test name="${test.suite.name}" />
		</junit>
	</target>
</project>