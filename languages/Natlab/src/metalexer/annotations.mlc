%component annotations

%append{
    //TODO-AC: hackish to add delimiter
    return symbol(ANNOTATION, "(*" + text, startLine, startCol, endLine, endCol);
%append}

Whitespace = [ \t\f\r\n]+

Letter = [a-zA-Z]
Digit = [0-9]
Identifier = ([_$] | {Letter}) ([_$] | {Letter} | {Digit})*

Number = 0 | [1-9] {Digit}*

Comment = [/][/] .*

%%

"." {: append(yytext()); :}

";" {: append(yytext()); :}
"," {: append(yytext()); :}

"#" {: append(yytext()); :}
"=" {: append(yytext()); :}
":" {: append(yytext()); :}

\( {: append(yytext()); :}
\) {: append(yytext()); :}
\{ {: append(yytext()); :}
\} {: append(yytext()); :}
\[ {: append(yytext()); :}
\] {: append(yytext()); :}

"*" {: append(yytext()); :}
"@" {: append(yytext()); :}
"$" {: append(yytext()); :}

"->" {: append(yytext()); :}
"?" {: append(yytext()); :}

"|" {: append(yytext()); :}
"&" {: append(yytext()); :}

"+" {: append(yytext()); :}
"-" {: append(yytext()); :}

%:

{Whitespace} {: /* skip */ :}
{Comment} {: /* skip */ :}

{Number} {: append(yytext()); :}

{Identifier} {: append(yytext()); :}

%:

<<ANY>> {: error("Illegal character '" + yytext() + "'"); :}
<<EOF>> {: error("Unterminated annotation"); :}